name: Build ImmortalWrt x86_64 1GB IPv6

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential libncurses-dev git flex gawk libssl-dev rsync wget unzip zstd

      - name: Clone ImmortalWrt
        run: |
          git clone https://gitee.com/immortalwrt/immortalwrt.git --depth=1 openwrt

      - name: Update feeds only official
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Build config
        run: |
          cd openwrt
          cat > .config <<EOF
CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_64=y
CONFIG_TARGET_x86_64_DEVICE_generic=y

CONFIG_TARGET_ROOTFS_PARTSIZE=1024
CONFIG_TARGET_ROOTFS_EXT4=y
# CONFIG_TARGET_ROOTFS_SQUASHFS is not set

# IPv6
CONFIG_IPV6=y
CONFIG_PACKAGE_dnsmasq-full=y
CONFIG_PACKAGE_odhcp6c=y
CONFIG_PACKAGE_odhcpd-ipv6only=y
CONFIG_PACKAGE_luci-proto-ipv6=y
CONFIG_PACKAGE_ipv6helper=y

# Language
CONFIG_LUCI_LANG_zh_Hans=y

# Basic
CONFIG_PACKAGE_luci-app-opkg=y
CONFIG_PACKAGE_luci-app-firewall=y
CONFIG_PACKAGE_luci-app-upnp=y
CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_luci-app-filetransfer=y
CONFIG_PACKAGE_luci-app-samba4=y
CONFIG_PACKAGE_luci-app-wol=y

# AdGuardHome
CONFIG_PACKAGE_luci-app-adguardhome=y

# MosDNS
CONFIG_PACKAGE_luci-app-mosdns=y

# Watchcat
CONFIG_PACKAGE_luci-app-watchcat=y

# ZeroTier
CONFIG_PACKAGE_luci-app-zerotier=y
CONFIG_PACKAGE_zerotier-one=y

# USB & Storage
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-fs-ntfs3=y
CONFIG_PACKAGE_block-mount=y
EOF

          make defconfig

      - name: Download
        run: |
          cd openwrt
          make download -j$(nproc)

      - name: Compile
        run: |
          cd openwrt
          make -j1 V=s

      - name: Collect files
        run: |
          mkdir -p firmware
          cp openwrt/bin/targets/x86/64/* firmware/ 2>/dev/null || true

      - name: Upload to Release
        uses: softprops/action_gh_release@v2
        with:
          tag_name: immortalwrt-x86_64-1gb-$(date +%Y%m%d%H%M)
          name: ImmortalWrt x86_64 1GB IPv6
          files: firmware/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
