name: Build ImmortalWrt x86_64 1GB 完整版

on:
  workflow_dispatch:
  repository_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout 仓库
        uses: actions/checkout@v4

      - name: 安装编译依赖
        run: |
          sudo apt update
          sudo apt install -y build-essential libncurses-dev git flex gawk libssl-dev rsync wget unzip zstd

      - name: 克隆 ImmortalWrt 源码
        run: |
          git clone https://github.com/immortalwrt/immortalwrt.git --depth=1 openwrt

      - name: 添加第三方软件源
        run: |
          cd openwrt
          echo "" >> feeds.conf.default
          echo "src-git daji https://github.com/kongfl888/openwrt-package.git" >> feeds.conf.default
          echo "src-git small https://github.com/kiddin9/openwrt-packages.git" >> feeds.conf.default

      - name: 更新 feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 生成编译配置
        run: |
          cd openwrt
          cat > .config <<EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_DEVICE_generic=y

          # 1GB 可用空间
          CONFIG_TARGET_ROOTFS_PARTSIZE=1024
          CONFIG_TARGET_ROOTFS_EXT4=y
          # CONFIG_TARGET_ROOTFS_SQUASHFS is not set

          # 中文
          CONFIG_LUCI_LANG_zh_Hans=y

          # 基础
          CONFIG_PACKAGE_luci-app-opkg=y
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_luci-app-ttyd=y
          CONFIG_PACKAGE_luci-app-filetransfer=y
          CONFIG_PACKAGE_luci-app-samba4=y
          CONFIG_PACKAGE_luci-app-wol=y

          # 大吉
          CONFIG_PACKAGE_luci-app-daji=y
          CONFIG_PACKAGE_luci-app-daji-config=y

          # AdGuardHome
          CONFIG_PACKAGE_luci-app-adguardhome=y

          # MosDNS
          CONFIG_PACKAGE_luci-app-mosdns=y
          CONFIG_PACKAGE_mosdns=y

          # OpenClash
          CONFIG_PACKAGE_luci-app-openclash=y

          # rtp2httpd
          CONFIG_PACKAGE_luci-app-rtp2httpd=y
          CONFIG_PACKAGE_rtp2httpd=y

          # Watchcat
          CONFIG_PACKAGE_luci-app-watchcat=y

          # EasyTier
          CONFIG_PACKAGE_luci-app-easytier=y
          CONFIG_PACKAGE_easytier=y

          # ZeroTier
          CONFIG_PACKAGE_luci-app-zerotier=y
          CONFIG_PACKAGE_zerotier-one=y

          # 存储驱动
          CONFIG_PACKAGE_kmod-usb-storage=y
          CONFIG_PACKAGE_kmod-fs-ext4=y
          CONFIG_PACKAGE_kmod-fs-ntfs3=y
          CONFIG_PACKAGE_block-mount=y
          EOF

          make defconfig

      - name: 下载依赖
        run: |
          cd openwrt
          make download -j$(nproc)

      - name: 编译固件
        run: |
          cd openwrt
          make -j$(nproc) || make -j1 V=s

      # 打包固件
      - name: 整理固件
        run: |
          mkdir -p firmware
          cp -f openwrt/bin/targets/x86/64/* firmware/ 2>/dev/null || true

      # 上传到你仓库的 Release
      - name: 上传到 GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: immortalwrt-x86-64-1gb-20260322
          name: ImmortalWrt x86_64 1GB 完整版
          body: |
            自动编译固件
            - 源码：ImmortalWrt
            - 架构：x86_64
            - 空间：1GB EXT4
            - 插件：大吉、AdGuardHome、MosDNS、OpenClash、EasyTier、ZeroTier 等
          files: firmware/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
